{
  "name": "ndjo-tacos",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm ci --prefix backend && npm run build --prefix backend",
    "start": "npm run start:railway --prefix backend"
  }
}
